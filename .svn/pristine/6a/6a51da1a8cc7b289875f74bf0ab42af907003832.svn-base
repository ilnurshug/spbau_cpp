#include <iostream>
#include <fstream>
#include "mergesort.h"
#include <cstdlib>
using namespace std;

 
int main()
{
    /*fstream in("input.txt");
    fstream out("output.txt");
    
    int* array = new int[10];
    int a;
    size_t n = 0;
    while (in >> a) 
    {
        array[n++] = a;
    }
    mergesort(array, n);
    
    for (size_t i = 0; i < n; ++i)
        out << array[i] << ' ';*/
    
    
    return 0;
}